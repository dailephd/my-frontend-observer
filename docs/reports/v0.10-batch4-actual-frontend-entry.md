# v0.10 Batch 4 Actual-Frontend Entry

## 1. Verdict and repository

PASS.

- Repository: `C:\Users\daile\Projects\my-frontend-observer`
- Original blocked starting commit: `a2d0cbd264516173355cd950f1319532d0b95e39`
- Planner clarification commit: `8451869ed235cfab802872edc581418162cbf678`
- Implementation commit: the commit containing this report, with message `feat: add v0.10 actual frontend visual change flow`; its final hash is recorded in the execution handoff.
- Branch: `feature/v0.10-full-visual-workflow`

## 2. Planner correction and retrieval

The original TST-B4-059 incorrectly expected equivalent repeated starts to retain one visual-change request identity. Canonical promotion deliberately creates a fresh contract instance, and Batch 1 request identity freezes that instance. The plan now records that repeated equivalent starts retain deterministic `contractRequestId` while producing different `contractId`, `visualChangeRequestId`, and `visualChangeWorkflowId`. No identity implementation or schema changed.

- Latest published my-dev-kit: `1.12.3`
- Install: `.my-dev-kit-workflow/tools/v0.10-batch4-resumed/`
- Fresh index: `.my-dev-kit-context/indexes/my-frontend-observer-v0.10-batch4-resumed-20260922`
- Exact command: `node C:\Users\daile\Projects\my-frontend-observer\.my-dev-kit-workflow\tools\v0.10-batch4-resumed\node_modules\@dailephd\my-dev-kit\dist\cli.js index --root C:\Users\daile\Projects\my-frontend-observer --src src --src viewer/src --src tests --src scripts --out C:\Users\daile\Projects\my-frontend-observer\.my-dev-kit-context\indexes\my-frontend-observer-v0.10-batch4-resumed-20260922 --call-graph --json`
- Index: 173 files, 1,726 symbols, 3,763 edges, no index-level warnings or errors. Data-model and classification analyzers remained partial with 812 and 1,673 warnings respectively; syntax, call graph, frontend semantic, and frontend reachability were complete.

Retrieval verified runtime draft/save ownership, explicit association and confirmation, canonical promotion eligibility, baseline reading, exact alias records, Batch 2 creation/activation, Batch 3 refresh/reselection, and the corrected identity hierarchy.

## 3. Server composition

`runtimeVisualChangeAuthoring.ts` implements the one project-aware composition adapter. `POST /api/annotations/:handle/start-visual-change` accepts only `{ itemIds }`, uses the shared Host, Origin, token, content-type, compression, body-size, no-store, and serialized-write boundaries, and fails closed in standalone mode.

Before promotion it resolves the supported saved runtime annotation, exact source observation, valid project config, configured valid persistent baseline contract, and an alias whose observation ID, request ID, and exact evidence-relative directory all match. It then calls `promoteVisualAnnotationContract` exactly once with the configured baseline ID. The adapter never calls legacy activation and project acceptance remains byte-identical.

The resulting `VisualChangeActualScope` freezes project-relative contained paths and canonical identities for the annotation, source observation, persistent baseline contract, and newly promoted contract. Creation delegates to `createProjectVisualChangeWorkflow`. A post-promotion workflow failure retains the immutable contract and returns bounded contract IDs with honest failure wording.

## 4. Viewer workflow

The existing runtime promotion selection now drives both legacy promotion and the distinct Create visual change action. Saved, clean, selected, confirmed promotable intent is required. Candidate, uninterpreted, inspect, remove, missing-association, and mismatched primitives remain non-executable. Legacy promotion and start disable one another while pending; the legacy activation checkbox affects only legacy promotion.

Successful start displays contract/workflow identities and `Activation: not yet activated`, refreshes the evidence index, switches the App-owned mode to Visual changes, and requests one-shot exact workflow-ID selection. Missing exact revisions warn without fuzzy selection. The new workflow has no activation, attempts, or governance results. The existing Activate control creates the immutable activation revision; no check is invoked automatically.

## 5. Identity and evidence proofs

Tests prove repeated equivalent starts have equal `contractRequestId`, unequal `contractId`, unequal `visualChangeRequestId`, and unequal `visualChangeWorkflowId`, with two persisted contracts and unchanged acceptance. A saved annotation revision changes visual-change request identity. Selecting different executable semantics changes both contract request and visual-change request identity.

A mixed saved annotation containing promotable intent, confirmed remove, and inspect evidence successfully creates a contract from only the selected promotable items. Remove and inspect remain annotation evidence and never become clauses.

## 6. Tests and validation

Focused server/application tests cover prerequisite-first rejection, selected subsets, exact scope, identity behavior, partial success, standalone/security behavior, and Batch 1/2 composition regression. The independently runnable real-Chromium ACCEPTANCE GATE creates fresh project evidence, authors and confirms runtime intent, saves it, starts an inactive workflow, opens the exact workflow, explicitly activates it, verifies unchanged request/new workflow identity, updates only the configured change contract, and confirms zero attempts/no automatic check.

Final validation passed: `npm run typecheck`, `npm run lint`, `npm test` (94 files, 1494 tests), `npm run test:browser` (30 files, 278 tests), `npm run test:security` (16 unit files/168 tests, 3 browser files/77 tests, plus its nested PWA hard gate), `npm run test:pwa-hard-gate` (1 passed, 6 filtered), `npm run build`, `npm run check:docs`, and `git diff --check`. The aggregate browser/security runs used Playwright API diagnostics because the unchanged service-worker checks timed out under this Windows host's normal scheduling but passed deterministically with diagnostic scheduling enabled.

## 7. Full-file read accounting

Full source/test file reads: none. Bounded my-dev-kit search, lookup, slice, exact source, continuation, dependency expansion, and explicit line ranges supplied all required context.

## 8. Exclusions and Batch 5 handoff

No reference-driven entry, reference selection/binding authoring, handoff, orchestrator integration, source editing, automatic check/retry, review/correction/acceptance, governance action, protocol/schema change, version bump, release, publication, or PR was implemented.

Batch 5 can add only the frozen reference-driven entry flow, reusing the Batch 2 workflow owner, Batch 3 secure workspace/reselection, and the explicit activation boundary preserved here.
