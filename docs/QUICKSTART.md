# Quickstart

Prerequisites are Node.js 24 or later and npm.

```powershell
npm install
npm run typecheck
npm run lint
npm test
npm run build
npm run check:docs
```

This validates the greenfield project foundation. There is no working product
observation command. Read [ROADMAP.md](ROADMAP.md) v0.1 before designing the
first implementation plan.
