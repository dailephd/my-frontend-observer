# Changelog

## Unreleased

- Established the private Node.js 24+ TypeScript CLI-profile foundation and
  standardized project documentation.
- Added complete repository-local Project Description and Project Milestones
  authorities plus the v0.1–v0.10 ROADMAP.
- Removed browser-observation product behavior that a historical greenfield run
  implemented before roadmap v0.1 planning was authorized.
- Implemented v0.1, Runtime Observation Foundation: a real `observe` CLI
  command that launches Chromium, enforces a loopback-only safety policy,
  captures a viewport screenshot plus bounded page/target evidence, and
  persists it as one portable `manifest.json` + `screenshot.png` artifact
  (schema `1.0.0`). Validated as a packed npm tarball in a clean consumer
  environment.

Roadmap v0.1 is implemented (source checkout and packed-tarball validated).
No public release has occurred.
